export interface UserEntryType {
  details: {
    firstName: string;
    lastName: string;
    email: string;
  };
  password: string;
}
export interface UserType {
  id: string;
  details: {
    firstName: string;
    lastName: string;
    email: string;
  };
}

export interface loginEntry {
  email: string;
  password: string;
}

// debts
// sales
// products
